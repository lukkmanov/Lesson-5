#include "function.h"

using namespace std;

int main()
{
    int arr[5] = {3,2,1,3,3};
    int b,n;
    cout << "Enter the length of the array" << endl ;
    cin >> b ;
    cout << "Enter how many to shift array" << endl;
    cin >> n;
    massiv(b);
    mas();
    CheckB(arr);
    shift(n);
    return 0;
}
